# Database boundary

Phase 1 PostgreSQL access, Drizzle schema, tenant transactions, forced RLS, function-only maintenance/rate-limit/key-rewrap/provider-usage capabilities, and migrations live here and in `/drizzle`. The executable subset covers accounts, one identity, encrypted identifiers, verification, consent, audit, deletion receipts, pseudonymous rate-limit windows, and synthetic provider usage reservations. Finding, observation, evidence, scans, provider configuration, jobs, remediation, notifications, and suppression remain conceptual in `docs/DATA_MODEL.md`.
