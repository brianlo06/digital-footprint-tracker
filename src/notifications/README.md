# Notification placeholder

The MVP defaults to dashboard-only notifications. Email verification now has a delivery-neutral gateway interface, but its only implementation is a local fake that sends nothing. No email, push, or SMS provider integration exists.
