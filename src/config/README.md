# Configuration placeholder

Server-only typed configuration validation lives here. Phase 1 consumes the database, auth-mode, local verification, and encryption settings in `.env.example`; provider, notification, feature-flag, and observability variables remain disabled placeholders.
