# Application shell placeholder

The Phase 1 Next.js foundation routes and UI composition live here: public explanation, explicit onboarding, identifier setup/local verification, privacy controls, and a dashboard. The dashboard now also carries a synthetic-only, user-triggered breach-metadata scan trigger and history display (`(protected)/dashboard`); no broker, owned-asset, remediation, or non-breach finding screen is implemented. The future information architecture remains in `docs/PRODUCT.md`.
